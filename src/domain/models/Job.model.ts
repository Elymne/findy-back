/**
 * @description
 * Representation of the structure of a Job data.
 * This data structure is used by Offer data structure.
 * I need this data structure because ppl may need to search offer by Job Type.
 */
export default interface Job {
    id: string
    title: string
}
